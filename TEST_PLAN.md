# 實際使用驗收標準（Google Meet 場景）

目的：以「demo 當天會不會出包」為準，而不是程式碼測試是否通過。
自動化：`cd api && uv run python -m e2e.run`（對運行中的 API 用 TTS 模擬兩位說話者 + 合成畫面；
硬規則 + LLM 裁判；報告在 `api/e2e/results/`）。標「自動」的項目每次改動後都要重跑；
標「手測」的項目是瀏覽器授權 / 真人聲音相關，賽前手動驗一次。
裁判校準：`uv run python -m e2e.calibrate`（把一個通過的場景故意弄壞三種：speaker 對調、少一個數字、
少最後一句，LLM 裁判必須全部判 FAIL）。

## 前置

- `api/.env`：`MOCK_MODE=false`、`LIVE_PROVIDER=openai`，`/health` 回 `live_provider: openai`。
- 兩台裝置或兩個帳號進同一個 Meet；本機瀏覽器開 `http://localhost:3000`。
- 點 **Start Meeting** → 選 Meet **分頁** + 勾 **分享分頁音訊** → 允許麥克風。
- Demo 開喇叭：A5 專門測回音去重。

## A. 說話者分離（核心）

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| A1 | 只有我講 | 每行 speaker 皆為「我」，內容對應 | 自動 |
| A2 | 只有與會者講 | 每行 speaker 皆為「與會者」 | 自動 |
| A3 | 兩人一句一句交替 | 每句歸屬正確、順序正確（依開口時間） | 自動 |
| A4 | 兩人**同時**講 | 各自獨立行、內容不互相混入 | 自動 |
| A5 | **開喇叭**，與會者講話從我的麥克風漏回 | 「我」不出現與會者的句子；`echo_dropped` 狀態事件記下被丟掉的回音 | 自動（合成回音：衰減 0.35、延遲 80ms、低通）+ 手測真喇叭 |
| A6 | 中英夾雜 | 逐字稿為繁體，不出現簡體 | 自動 |
| A7 | 句中 0.4s 停頓 | 一行 | 自動 |
| A8 | 句中 1s 思考停頓 | 可切行，但同一人、資訊完整、紀錄一致 | 自動 |

## B. 視覺指涉與決策

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| B1 | 有畫面，與會者說「右邊這塊圖表先不要動」 | grounded event 指向右側圖表、附截圖、speaker=與會者 | 自動 |
| B2 | 「我們決定採用方案 B」 | 一個候選決策，chosen 含方案 B，不重複 | 自動 |
| B3 | 沒有畫面時說「這個」 | **不**產生 grounded event | 自動 |
| B4 | 決策與知識庫衝突（Prototype B 超上限） | conflict alert 含 1,020 > 850 | 自動 |
| B5 | 閒聊 | 無 decision / alert / grounded | 自動 |

## C. 失敗情境要「看得到」

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| C1 | 未勾「分享分頁音訊」 | 立即 toast 提示 | 已實作，手測 |
| C2 | 拒絕麥克風 | toast 提示，與會者通道仍正常 | 已實作，手測 |
| C3 | 分享「整個螢幕」而非分頁 | 仍能運作 | 手測 |
| C4 | 網路中斷 / OpenAI 錯誤 | 狀態變 Ended 並有 toast | 手測 |
| C5 | API 重啟後 | `GET /sessions/{id}`、`/record`、`/report`、截圖仍可取得（從硬碟重建） | 單元測試 |

## D. 紀錄與報告（資訊不掉、依頁歸檔）

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| D1 | 會後 Generate Report | 決策表含會中決策、引用 speaker 正確 | 自動 |
| D2 | **十句混合對話 + 中途翻頁** | 紀錄 = 逐字稿一一對應、無 pending、決策/指涉連回觸發句；10 輪順序正確；報告 key_facts 含全部數字/時程/人名/待辦；**恰好 1 個 grounded**；decision 事件 ≤ 4、理由 ≤ 12 條、同來源 conflict ≤ 1；**2 頁**、每頁封面截圖可取得、每個事實落在正確頁或相鄰頁、報告兩頁各有 title/summary | 自動 |
| D3 | 翻頁前後講不同主題 | 不要求頁數精準，但「講成本上限那頁」要找得到：第 1 頁 summary 含成本，第 2 頁含時程 | 自動 |

### 頁（scene）的規則

- 分享畫面每張 frame 算 dHash；相近（≤10 bit）併同頁，否則開新頁；< 3s 又回到前一頁樣子的閃切會被併回。
- 每句話記 `scene_id`（開口當下畫面）；開口時間離頁面切換 ±4s 內的句子另記 `adjacent_scene_ids`。
- 驗收只要求「正確頁或相鄰頁」；報告 `pages` 把邊界句同時列在兩頁（`also_in`）。
- 不辨識簡報頁碼；索引靠會後 Luna 為每頁寫 title / summary。

## 從實測反推已修的問題（時間序）

- 混音 + 能量判斷會把同時說話併成一行 → 每個說話者一條獨立轉錯連線（A4）。
- 轉錄輸出簡體 → 轉錄 prompt + 詞彙表（A6）；STT A/B 後仍維持 gpt-4o-mini-transcribe（唯一原生繁體）。
- 固定毫秒 VAD 不穩健 → `semantic_vad`（A7）；1s 思考停頓仍會切行，改為讀取端合併片段（A8）。
- 時間戳用轉錄完成時間導致順序錯亂 → 用 `speech_started.audio_start_ms` 算開口時間（D2）。
- 同一件事被重複提成多個決策 → session 層 rapidfuzz 合併 + prompt 只在拍板時提案（B2/D2）。
- 報告把「成本壓到 920」補上錯誤主詞 → key_facts 加 `quote`，fact 只能改寫原句（D2）。
- 一個 prompt 要 luna 做七件事 → 拆 extract / coverage / derive / scene-index 階段（D1/D2/D3）。
- Realtime 推理連線在只收文字後失去意義 → 換 Responses API 每句無狀態呼叫（B/D 全數回歸）。
- gpt-5.4-mini 有畫面時過度建 anchor（D2 十句出 12 個；改用模型 confidence≥0.85 放行也一樣 12 個）
  → 指示語 / 畫面詞是硬條件，模型信心不採用（B3/D2 grounded_max）。
- gpt-5.4-mini 把「先看成本」「以滿意度評估」提成決策、chosen 寫「尚未拍板」 → 觸發句必須有拍板詞
  （決定/採用/就用/改成…）且 chosen 不得含「尚未/候選/評估中」（D2 decision 事件 5 → 2）。
- 同一決策每重述一次就重跑衝突檢查、alert 疊 5 條 → 只在選項改變時查；同 (決策, 來源) 只保留一條並更新內容。
- 理由清單靠字串比對去重，改寫一次就多一條（18 條） → rapidfuzz 語意去重，但數字/選項字母不同絕不合併；每清單最多 6 條。
- 開喇叭時與會者的話出現在「我」 → `me` 句先扣 1.5s（遠端還在講就最多等到 8s），
  與 4s 內的遠端句相似（≥80）即丟棄並記 `echo_dropped`（A5）。
- 報告把「下週三」換算成日期、把知識庫內容寫成會中說的 → prompt 明令時間照原話、引用知識庫要標來源。
- 測試 harness 本身的失真：同人音訊交錯、TTS 首尾靜音、句間沒有持續送靜音導致 VAD 不收尾與時間漂移、
  在伺服器落檔前就抓紀錄、synthesis 前就抓紀錄看不到頁 title → 全部修正。

## 第一次實機 Google Meet 測試反推已修（2026-09-05 凌晨）

- 沒人講話時轉錄模型把 prompt 詞彙表編成 234 字假會議段落，推理層據此建了假決策
  → 能量閘（音訊沒到人聲等級就丟）+ 詞彙回吐偵測（長段落且 ≥3 詞 / 任一句 ≥6 詞）+ 詞彙表縮短；記 `stt_rejected`。
- 「這個是貓咪杯子」：逐字稿晚 8–12 秒才到，推理拿到的是杯子放下後的畫面，仍給 conf 0.22 的 anchor；
  「以及這個是…」半句就錨了「小物件」；「這個杯子公差」錨到人臉 conf 0.37
  → 擷取與推理解耦：每 2 秒固定截圖 + 伺服器時鐘；推理分「聽」「看」兩步，
    「看」只撈開口→收口區間的圖（找不到往前 10s），視覺去找語音講的名字；信心 <0.6 不建；半句不建。
    用那場的錄音重放（`e2e.replay`）：手把 ✓、貓咪杯子 ✓（frame 32.8s，非 43s）、指甲剪 ✓（target 帶名字）、
    半句 → not_visible、人臉 0 次。
- 瀏覽器截圖時間戳是頁面開啟時間（差 38s）→ 伺服器蓋章。
- 「右邊這塊圖表顯示 B 最高」被聽成「描述數據」而沒去看 → 指示語為決定性條件，聽的一步漏了也強制去看。
- 同一個手把講三句出兩個 anchor；anchor 只寫外觀，「新產品、9/29 推出」沒進去 → 「聽」的一步先判斷
  這句指的是已錨定的哪一個（refers_to）還是新東西，並抽出這句對它說了什麼（about）；同一個東西只更新、
  不重看圖，`said[]` 累積語音資訊，卡片顯示。重放：1 個 anchor，said=[是個手把, 是我們的新產品, 9月29號會推出]。
- PRD 寫「下週三」而逐字稿是「9月29號」→ 根因是**我在 prompt 裡放的範例「下週三」被當成內容**。
  全部 prompt 移除具體日期範例（單元測試守住）；會議日期餵給 Luna，key_facts 加 `resolved_date`（YYYY-MM-DD）；
  PRD/工作項只准用 resolved_date；程式再掃一次報告，對不回逐字稿的日期寫進 uncertainties。
- 第二個人與主持人同房 → 全部歸「我」（麥克風直接收到）。預期行為；測與會者通道對方要在別處。

## 已知限制

- 語料為 TTS 合成；真人口音需手測（`e2e/audio/<name>.wav` + step `"clip": "<name>"` 可改用真人錄音）。
- 回音去重是文字層比對：與會者講很短的話（≤2 字）不比對；遠端句還沒轉完就過了 8s 的極端情況會漏。
- STT 同音錯字（滿意度→埋度、握感→握趕）仍會發生；報告層靠上下文修正（quote 保留原文），不保證。
- 遠端多人共用「與會者」一個通道；分不出遠端 A / B。
- Gemini legacy 引擎（`LIVE_PROVIDER=gemini`）未跟上雙通道架構，不建議使用。
