# 實際使用驗收標準（Google Meet 場景）

目的：以「demo 當天會不會出包」為準，而不是程式碼測試是否通過。
每一項都要在真實 Google Meet 會議中手動驗證；失敗就回頭改程式。

## 前置

- `api/.env`：`MOCK_MODE=false`、`LIVE_PROVIDER=openai`，`/health` 回 `live_provider: openai`。
- 兩台裝置或兩個帳號進同一個 Meet；本機瀏覽器開 `http://localhost:3000`。
- 點 **Start Meeting** → 選 Meet **分頁** + 勾 **分享分頁音訊** → 允許麥克風。
- 建議戴耳機；A5 專門測喇叭。

## A. 說話者分離（核心）

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| A1 | 只有我講 10 秒 | 每行 speaker 皆為「我」 | 待手測 |
| A2 | 只有與會者講 10 秒 | 每行 speaker 皆為「與會者」 | 待手測 |
| A3 | 兩人一句一句交替 | 每句歸屬正確、順序正確 | 待手測 |
| A4 | 兩人**同時**講 5 秒 | 各自產出獨立行、內容不互相混入 | 合成語音驗證通過 |
| A5 | 用喇叭、與會者講話 | 不出現內容相同的「我」行（回音） | 待手測 |
| A6 | 中文語音 | 逐字稿為繁體，不出現簡體 | 合成語音驗證通過 |
| A7 | 一句話中間自然停頓 | 不會被切成多行破碎片段 | 合成語音驗證通過 |

## B. 視覺指涉與決策

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| B1 | 分享含圖表的畫面，與會者說「右邊這塊圖表先不要動」 | 3 秒內出現 grounded event，附截圖、speaker=與會者 | 待手測 |
| B2 | 任一人說「我們決定採用方案 B」 | Decision 面板出現候選，chosen 含「方案 B」 | 合成語音驗證通過 |
| B3 | 沒有畫面時說「這個」 | **不**產生 grounded event（不亂猜） | 合成語音驗證通過 |
| B4 | 決策與知識庫衝突 | Alerts 出現 conflict | 待手測 |
| B5 | 閒聊 30 秒（無決策） | 不出現多餘 decision / alert | 待手測 |

## C. 失敗情境要「看得到」

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| C1 | 未勾「分享分頁音訊」 | 立即 toast 提示，與會者不會安靜漏掉 | 已實作 |
| C2 | 拒絕麥克風 | toast 提示，與會者通道仍正常 | 已實作 |
| C3 | 分享的是「整個螢幕」而非分頁 | 仍能運作（Windows 整螢幕含系統音） | 待手測 |
| C4 | 網路中斷 / OpenAI 錯誤 | 狀態變 Ended 並有 toast，不會卡 Live | 待手測 |

## D. 報告

| # | 操作 | 通過條件 | 現況 |
|---|------|---------|------|
| D1 | 會後 Generate Report | 報告中的引用帶正確 speaker，決策表對應 B2 | 待手測 |
| D2 | 報告耗時 | < 30 秒 | 待手測 |

## 從實測反推已修的問題

- 混音 + 能量判斷會把同時說話併成一行 → 改為每個說話者一條獨立轉錄連線（A4）。
- 轉錄輸出簡體 → 加轉錄 prompt、推理指令強制繁體（A6）。
- 句子被 VAD 切碎 → `silence_duration_ms` 700（A7）。
- Mock 模式丟掉所有音訊 → `.env` 切 openai 並在 UI 顯示 provider。
